import { it } from "vitest";

// Placeholder test file for packages/db.
// Real integration tests require a live DATABASE_URL (see docker-compose.yml).
// Sprint 1 action (Ingrid): add integration tests reaching 75% coverage.
// Sprint 2 retro: activate hard threshold enforcement in CI.

it.todo("add DB integration tests");
